<% if @vote.valid? %>
  $("#comment_<%= @comment.to_param %>_votes").html("<%= @comment.total_votes %> votes")
<% else %>
  $("#comment_<%= @comment.to_param %>_votes").parent().before(alert_error("You can only vote once"))
<% end %>
s
