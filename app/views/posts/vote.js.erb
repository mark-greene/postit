<% if @vote.valid? %>
  $("#post_<%= @post.to_param %>_votes").html("<%= pluralize(@post.total_votes, 'vote') %>")
<% else %>
  $("#post_<%= @post.to_param %>_votes").parent().before(alert_error("You can only vote once"))
<% end %>
